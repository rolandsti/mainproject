from django.db import models

# models importet by default
# Create your models here.

#class content(models.Model):
#	title		= models.CharField(max_length=100) #search for modal field reference
#	destription = models.TextField(default='this is awesome')